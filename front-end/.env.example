#####################################################################
# Connecttr frontend (Create React App) env
# - Copy to `front-end/.env` and fill values.
# - Variables must start with REACT_APP_ to be injected.
#####################################################################

# Backend base URL for local dev when using start-local.bat
REACT_APP_API_BASE=http://127.0.0.1:8010
